import styled from "styled-components"
import { colors, fonts, fontSizes } from "../style";

const IntroCaption = styled.div`
    font-family: ${fonts.serif};
    font-size: ${fontSizes.large};
    text-align: left;
    line-height: 50px;
    color: ${colors.text};
`

export default IntroCaption; 